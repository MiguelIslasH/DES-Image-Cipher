# DES-Image-Cipher
Here's a tool for encrypt and decrypt BMP images using DES

# 🏁 Requirements
```bash
    PIL:
    pip install Pillow

    hash:
    pip install hashlib

    pycripto:
    pip install pycrypto
    ```
